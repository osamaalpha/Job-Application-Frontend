name: jobs_dashboard
description: Small admin dashboard for job applications
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  dio: ^5.5.0
  flutter_riverpod: ^2.5.1
  fluttertoast: ^8.2.6
  logger: ^2.3.0
  material_design_icons_flutter: ^7.0.7296

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
uses-material-design: true
